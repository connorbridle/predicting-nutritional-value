public class NeuralNetwork {

    //Constants and other variables that will be used
    private static int NUMBER_OF_ITERATIONS = 10000;

    public NeuralNetwork() {

    }

    //The main meat of the code. This function will train the neural network by iterating a set number of times
    //and apply weight adjustments where necessary
    public void trainNetwork() {
        for (int i = 0; i < NUMBER_OF_ITERATIONS; i++) {
            //Pass the training data-set through the network

            //Calculate the error

            //Update the weights
        }
    }
}
